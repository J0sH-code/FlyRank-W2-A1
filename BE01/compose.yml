services:
  api:
    build: .
    ports: ["3000:3000"]
    command: npm run dev
    environment:
      HOST: "db"
      USER: "${USER}"
      DB_PORT: 5432
      PASSWORD: "${PASSWORD}"
      DATABASE: "${DATABASE}"
      SUPABASE_URL: "${SUPABASE_URL}"
      SUPABASE_KEY: "${SUPABASE_KEY}"
      PORT: 3000
    depends_on: [db]
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: "${USER}" 
      POSTGRES_PASSWORD: "${PASSWORD}"
      POSTGRES_DB: "${DATABASE}"
    ports: 
      - "5433:5432"
    volumes: [taskdb_data:/var/lib/postgresql]
volumes:
  taskdb_data: